#include <stdio.h>

int main(){
           int i[100];
           int num, limit;
           printf ("enter array limit")
           printf ("enter number")
           for (i = 0; i < limit; i++){
           scanf ("%d", &num[i])
           }
           for (i = 0 ; i < limit; i++){
           printf ("you have ender %d \n", num[i])
           }
          }
